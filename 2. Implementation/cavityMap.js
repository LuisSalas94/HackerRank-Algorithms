/* https://www.hackerrank.com/challenges/cavity-map/problem
  https://www.youtube.com/watch?v=eOfU3CPQYrw&ab_channel=HackersRealm
*/
function cavityMap(grid) {
	/* 	// Write your code here

	//step0
	const length = grid[0].length;

	//step1
	let numGrid = grid
		.map((cell) => cell.split(""))
		.flat()
		.map((cell) => Number(cell));
	console.log(numGrid);

	//stpe3
	let maxVal = Math.max(...numGrid); */
}

console.log(cavityMap(["1112", "1912", "1892", "1234"]));
